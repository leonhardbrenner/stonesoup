####Write the generated interfaces and dtos into the correct directory.
####Delete the old and make it work again.
#Make that configurable

Create an upgrade script to keep external projects current. At a minimum we should have a way of knowing when each package is upgraded to tech debt is accounted for.

https://www.jetbrains.com/space/solutions/software-teams/#space-form

Pull changes from main into our improve-plan branch.
